# Single upstream RPC provider for now (see docs/RESEARCH.md, "upstream
# dependency — resolved"). Target 2-3 providers with failover once real
# traffic justifies it.
RPC_URL=https://ethereum-rpc.publicnode.com
